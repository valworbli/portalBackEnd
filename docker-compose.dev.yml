version: "3"

services:

  portalbackend:
    links:
      - portalbackend-mongo
    ports:
      - '9229:9229'
      - '5000:5000'
    environment:
      NODE_ENV: production
    command:
      bash -c "npm install && npm run dev"
      # bash -c "npm run import"
      # bash -c "npm run test"
