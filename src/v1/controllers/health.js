
/**
 * Set Applicant
 * @param {string} req - The incoming request.
 * @param {string} res - The outcoming response.
 */

function getCheck(req, res) { // eslint-disable-line
  res.status(200).send('OK');
}

module.exports = {getCheck};
